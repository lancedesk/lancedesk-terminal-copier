# npm Wrapper

This package is a discoverability wrapper for `ld`.

## Usage

```bash
npx lancedesk-terminal-copier ld-install-release 0.1.0
```

It fetches and runs the project's signed/checksummed release installer script.
