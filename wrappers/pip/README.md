# pip Wrapper

This package is a discoverability wrapper for `ld`.

## Usage

```bash
pip install lancedesk-terminal-copier
ld-install-release 0.2.0
```

It downloads and executes the project's release installer script.
